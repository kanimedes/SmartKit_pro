class Option {
  String? id;
  String? options;
  String? counter;
  String? percentage;
  String? questionId;

  Option(
      {this.id, this.options, this.counter, this.percentage, this.questionId});
}
